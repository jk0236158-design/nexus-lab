{
  "name": "@nexus-lab/zen-memory-server",
  "version": "0.1.0",
  "description": "Zen's persistent memory MCP server — stores decisions, learnings, knots, and session metrics",
  "type": "module",
  "bin": {
    "zen-memory-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "sql.js": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
