---
title: minimal template
description: Smallest viable MCP server — single tool, stdio transport
---

# minimal

<!-- Buyer path placeholder (Akari fills in, optional for free templates). Free templates may skip the 6-point block. -->

> **Note for Akari**: README for this template does not yet exist. Once `packages/create-mcp-server/templates/minimal/README.md` lands, replace the block below with:
>
> ```
> <!--@include: ../../create-mcp-server/templates/minimal/README.md-->
> ```

## Status

README pending. Scaffold the template locally with the install command below to inspect the source.

## Install

```bash
npx @nexus-lab/create-mcp-server my-server --template minimal
```
